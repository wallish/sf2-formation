post_index:
    pattern: /post
    defaults: { _controller: "ESGIBlogBundle:Post:index" }

post_list:
    pattern: /post/list
    defaults: { _controller: "ESGIBlogBundle:Post:list" }

post_show:
    pattern: /post/show/{id}
    defaults: { _controller: "ESGIBlogBundle:Post:show" }